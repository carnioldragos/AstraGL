# AstraGL
GPU-accelerated video player (Electron + WebGL2) with a real-time shader pipeline: motion-compensated TAA, RCAS sharpening, deband, HDR tone mapping, and refresh-rate frame interpolation. Hardware decode via WebCodecs with FFmpeg fallback for legacy formats.
